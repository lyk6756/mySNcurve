#include "standardSN.h"

/**
 * 构造函数
 */

standardSN::standardSN(double SRI1, double b1, double Nc1, double b2, double SE, double RR, double Nfc)
    : sri1(SRI1), b1(b1), nc1(Nc1), b2(b2), se(SE), rr(RR), nfc(Nfc) {}

